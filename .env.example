# Human Design Hub API key (server-side only — NEVER exposed to the browser).
# Get it from https://humandesignhub.app developer dashboard (free plan: 100 credits/month).
# Copy this file to .env.local and fill the value. .env.local is gitignored.
HUMAN_DESIGN_API_KEY=
